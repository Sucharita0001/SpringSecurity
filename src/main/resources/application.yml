spring:
  application:
    name: security
#  security:
#    user:
#      name: admin
#      password: admin

server:
  port: 10

additional:
  users:
    user-name: ${BASIC_USER_NAME}
    user-password: ${BASIC_USER_PASSWORD}
    user-authority: ${BASIC_USER_AUTHORITY}
    admin-name: ${ADMIN_NAME}
    admin-password: ${ADMIN_PASSWORD}
    admin-authority: ${ADMIN_AUTHORITY}
